// Public entry for the in-page bridge component.
//   import { AgentBridge } from 'nextjs-agent-mcp/bridge';
//   {process.env.NODE_ENV === 'development' && <AgentBridge />}
export { default as AgentBridge } from './agent-bridge.client';
